import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["200","400", "700"]
});

export const metadata: Metadata = {
  title: "Bookly - Online Appointment Scheduling Platform",
  description: "Bookly is a full service online appointment scheduling platform that will capture leads, manage appointments, send notifications, and track customers with everything in one place.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${poppins.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
